// Package model contains the transport-independent metrics data model shared by
// the receiver, processors, and exporter. It deliberately has no dependency on
// OTLP protobuf or Prometheus types.
package model
